find a way to compare to curent time
possibly stored in a variable
if else statements for each hour to assign class by color
add click event to save input to local storage
find a way to get info from local storage and display on the mf page